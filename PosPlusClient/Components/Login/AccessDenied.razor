    <FluentStack HorizontalAlignment="HorizontalAlignment.Center" VerticalAlignment="VerticalAlignment.Center" Style="height: 85vh">
        <FluentMessageBar Title="ACCESO DENEGADO"
                          Intent="MessageIntent.Error"
                          Type="@MessageType.Notification"
                          AllowDismiss="@false">
            Usted no tiene Acceso a esta función. Contacte al Administrador.
        </FluentMessageBar>
        @* <FluentMessageBar Title="ACCESO DENEGADO" Intent="@MessageIntent.Error" AllowDismiss="false" Style="text-align: center;width: 250px"> *@
        @*     Usted no tiene Acceso a esta función. Contacte al Administrador. *@
        @* </FluentMessageBar> *@
    </FluentStack>
@code {
    
}